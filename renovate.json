{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:js-app", ":automergePatch"],
  "packageRules": [
    {
      // NOTE:
      // :automergeLinters というプリセットがあるが、
      // majorもautomergeしてしまうので個別に設定
      // https://docs.renovatebot.com/presets-default/#automergelinters
      "extends": ["packages:linters", ":automergeMinor"]
    }
  ]
}
